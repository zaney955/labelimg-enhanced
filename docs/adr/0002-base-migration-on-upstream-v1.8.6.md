# Base the migration on upstream v1.8.6

The independent repository will start from upstream tag `v1.8.6` at commit `1ab8241`, matching the currently installed LabelImg distribution. Local customizations and directory normalization will be applied as later, reviewable commits so that migration changes are not mixed with an upstream-version upgrade.
