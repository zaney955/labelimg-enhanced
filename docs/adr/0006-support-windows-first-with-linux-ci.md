# Support Windows first with Linux CI

LabelImg Enhanced will initially support Python 3.14 only and treat Windows and the `DL` Conda environment as the primary supported runtime, while retaining Linux compatibility through a Python 3.14 headless GitHub Actions test. macOS compatibility will remain best effort until a maintained macOS validation path is added, preserving cross-platform code without claiming an unverified support level.
